password=None
attempts=[]
while password !="chewbacca":
    password = input("Enter the password:")
    attempts.append(password)
    if password !="chewbacca":
        print("Your password is inccorrect.You have already guessed")
        for i in attempts:
            print(i)
print("Correct password.Welcome")    
